substitutions:
  _IMAGE: us-central1-docker.pkg.dev/$PROJECT_ID/poly-x-kalshi-dev/fifa-scanner:latest

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - docker/Dockerfile.gcp-scanner
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
